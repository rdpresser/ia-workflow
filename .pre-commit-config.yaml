repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.13
    hooks:
      - id: ruff
        args: [ --fix ]
      - id: ruff-format
  # NOTE: MyPy is disabled in pre-commit due to src-layout namespace resolution issues.
  # Run manually: poetry run mypy src/ai_taskflow/ --explicit-package-bases
  # This will be re-enabled after resolving the namespace package conflict.
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v2.1.0
  #   hooks:
  #     - id: mypy
  #       files: ^src/ai_taskflow/
  #       args: [--explicit-package-bases]
  #       additional_dependencies:
  #         - pydantic>=2.0.0
  #         - pydantic-settings>=2.0.0
  #         - types-requests
  #         - types-redis
